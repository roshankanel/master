// close the modal
$('.modal').modal('hide');
var item = <%= @user.to_json.html_safe %>;
$("#statusModal-<%= @user.id %>").modal("hide");
$(".row.row-msg").fadeOut().remove();
$(".messages").fadeOut().remove();
<% if controller.action_name == "update_approve" %>
  $("#"+ item["id"] +"").replaceWith("<%= escape_javascript(render partial: 'row_item', locals: {user: @user} ) %>");
<% end %>
