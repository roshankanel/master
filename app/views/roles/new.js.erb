var contents = "<%= escape_javascript(render partial: 'roles/form', locals: {url: roles_path, submit_label: 'Add', heading_prefix: 'Add', method: 'post'}) %>";

$("#modal-window").modal({backdrop: 'static', keyboard: false});
$("#modal-window").on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var action = button.data('action')
})
$("#modal-window").html(contents);
