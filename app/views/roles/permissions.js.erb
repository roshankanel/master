var contents = "<%= escape_javascript(render partial: 'roles/permissions', locals: {url: role_update_permissions_path, submit_label: 'Update', heading_prefix: 'Edit', method: 'patch'}) %>";
// remove any previous flash messages
$(".row.row-msg").fadeOut().remove();
$(".messages").fadeOut().remove();

$("#modal-window").modal({backdrop: 'static', keyboard: false});
$("#modal-window").on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  var action = button.data('action');
})

$("#modal-window").html(contents);
